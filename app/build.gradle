apply plugin: 'com.android.application'

android {
    compileSdkVersion 26
    defaultConfig {
        applicationId 'com.camerakit.jpegkit.app'
        minSdkVersion 15
        targetSdkVersion 26
        versionCode 1
        versionName '1.0'
    }
}

dependencies {
    implementation project(path: ':jpegkit', configuration: 'default')
    implementation 'com.android.support:appcompat-v7:26.1.0'
}
